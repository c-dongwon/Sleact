import React from 'react';
import { Container, Header } from './styles';

const Channel = () => {
    return (
        <Container>
            <Header>로그인했따!</Header>
        </Container>
    );
};

export default Channel;