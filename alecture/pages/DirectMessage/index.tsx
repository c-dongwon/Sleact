import Workspace from '@layouts/Workspace';
import React from 'react';

const DirectMessage = () => {
    return (
        <Workspace>
            로그인했따!
        </Workspace>
    );
};

export default DirectMessage;